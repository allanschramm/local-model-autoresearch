# 0020. Swap S1D4/S2D1 per SPEC slot map

Per SPEC (slot map) and issue #29: sampler lesson content moved to slot `s1d4`, skills (Matt Pocock chain) moved to slot `s2d1`. Filenames and slot ids unchanged. Quiz sets follow slots: samplers = `s1d4-q1`…`q4` (answer hashes recomputed), skills = `s2d1-q1`…`q3`. Nav titles/links updated in both lessons, `index.html`, `progress.js` (`LESSON_QUIZZES` + `LESSON_ORDER`), cross-refs in s1d3/s2d2, `CURRICULUM.md` statuses (swap publicado), `QUIZ-HASH.md`, and `sampler-sim.js` header comment.

**Implications:** `s1d4` gate = 4 quizzes + prática; `s2d1` gate = 3 quizzes + prática. Day/Night usage section on S1D4 still pending (issue #30). Student HTML kept zero meta leak (teach/AGENTS.md).
