# 0015. S2D3 guardrails publicado

Publicamos Semana 2 · Dia 3 (Allow/Deny/hooks Cursor+Claude, sandbox Claude vs Cursor no Windows, CLI `--tools`). Banner de rascunho removido; entra em `LESSON_ORDER` (9 aulas). S2D4 continua fora. Harness = vocabulário do aluno. “allowlist” = jargão (usar lista de permitidos); `--allowedTools` = flag CLI.

**Implications:** progresso e CTA do guia incluem s2d3; quiz gate exige q1–q4 + prática.
