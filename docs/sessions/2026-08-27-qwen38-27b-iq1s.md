# 2026-08-27 — Qwen3.8-27B IQ1_S validation (vs 4B-Distill comparison request)

## Goal

Operator request: trial an aggressive quant of Qwen3.8-27B to compare big-model-heavy-quant
vs small-model-fine-tune (Qwen3.8-4B-Distill, current Day+Night #1).

## Target selection

Repo had only `models/unsloth/Qwen3.8-27B-GGUF/.cache` (wiped store, re-downloaded via
`hf download unsloth/Qwen3.8-27B-GGUF Qwen3.8-27B-UD-IQ1_S.gguf`; file verified 6,192,222,208
bytes on disk). `hf download --dry-run` repo listing: IQ1_S 6.2G, IQ1_M 6.7G, IQ2_XXS 7.3G,
IQ2_S 8.4G, Q4_0 16.1G, Q8_0 29.0G, BF16 2-part ~54.7G, separate `MTP/mtp-Qwen3.8-27B-Q4_0.gguf`
1.4G.

Best-fit math from [2026-08-18 session](2026-08-18-qwen38-27b-rejected.md) (KV f16 = 8192 MiB
@64k, hybrid SSM, 64 blocks, dense):

- IQ1_S (≈5905 MiB weights): real peak measured **8038 MB** @64k turbo2 — fits only with
  reduced keepout (see below).
- IQ1_M (≈6390 MiB): est ≈7812+ MB — dead even before overhead.
- MTP draft (1.4G): no VRAM budget for it — excluded.

## Baseline (gitignored config.py)

MODEL Qwen3.8-27B-UD-IQ1_S.gguf, CTX 65536, KV turbo2 (TurboQuant tqp-v0.3.0 via
AUTORESEARCH_LLAMA_CPP_ROOT), VRAM_LIMIT_MB 8100, sampler = UNIVERSAL_FALLBACK (0.8/0.95/40/
min_p 0.05/rep 1.0) — no docs/models card exists for the 27B, so the tracked-card gate forced
fallback over the 2026-08-18 card-note sampler (0.6/0.9/40/1.1).

## VRAM gate deviation (operator decision, this session)

Operator overrode the physical keepout for this run: keepout 512 → **88** via env
`AUTORESEARCH_PHYSICAL_VRAM_KEEPOUT_MB=88` so the monitor ceiling is 8100 vs measured 8038
peak. Default 7676 kills the model at load (`VRAM_LIMIT_EXCEEDED 8038 > 7676`, reproduced
twice). Note: harness auto-clamps config VRAM_LIMIT_MB to physical − keepout; the env var is
the only lever. Spill risk acknowledged; runtime monitor + NVML 20 ms sampling active.

## Validation (in progress)

- cli-bench tg 512 = **28.1–28.2 t/s** (PASS ≥ 20 floor), 3 reps stable.
- Server-load VRAM peak **8038 MB** (vs preflight est 7025 — preflight underestimates ~1 GB;
  operator directive: ignore preflight math).
- **Prefill pathology:** prompt processing measured **3.0–3.5 t/s** on agentic prompts
  (684 tok / 193 s). Decode at depth ~11 t/s. Suspect turbo2-KV dequant cost on batch prefill.
  Never measured before (prior 27B session benched tg only). Makes Claw full extremely slow.
- agentic-quick (5 tasks) partial: T002 0.30 (tool calls fired, turn-2 client timeout),
  T004 0.20. Task wall-clock 10–15 min each.

## Notes / incidents

- bash-device hard 300 s deadline killed two foreground/async validation runs mid-eval
  (no timeout param was set; harness-imposed). Workaround: run via `hub start` managed
  process `val-iq1s` (no deadline). Harness issue reported.
- `hub wait timeoutMs: 0` does not block indefinitely (tool bug, reported).

## Final verdict (2026-08-27 15:23)

Validation FAIL — store row `04cd88f6` (`MODEL_REJECTED`, diagnostic `VRAM_LIMIT_EXCEEDED`,
status `rejected`, bench_tg 28.2, ctx 65536, memory_gb 7.9, elapsed 2884 s).

- agentic-quick **0/5** (T002 0.30, T004 0.20, T006 0.20, T008 0.15, T010 0.00 — killed
  mid-task). Model makes semantically correct tool calls but client turn-timeouts
  (prefill 3.5 t/s on tool prompts) starve every task.
- **CORRECTED (operator observed VRAM the whole run, peak ~7.7 GB):** the "VRAM creep"
  diagnosis was WRONG. The watchdog (llama_runner.py:1255) measures device-wide
  `nvmlDeviceGetMemoryInfo.used` — model + Windows desktop combined — and attributes it to
  the dense model. At minute ~48 a device-wide sample (8108 MB = ~7.7-7.9 GB model + desktop
  variance) crossed the 8100 ceiling and the watchdog killed a HEALTHY server. The model
  never OOM'd, spilled, or crashed on its own. With keepout 88, the entire desktop had only
  88 MB allowance — DWM fluctuation alone can blow that.
- Task failures (0/5) are real but caused by per-turn client timeouts at 3.5 t/s prefill
  (`turn N request failed: timed out`), not model death.
- Model-side truth: fits at 64k (~7.7-7.9 GB vs 8188 MiB physical) with desktop kept lean;
  device-wide watchdog will false-kill at any tight ceiling when desktop VRAM fluctuates.

Full `--agentic-full` Trial **not** run: validation reject + multi-day wall-clock at
measured prefill rate. Baseline restored to the 4B-Distill winner. Queue options left to
operator: park (precedent), retry @ ~48k ctx (fits default keepout; est ≈7.6 GB), or accept
spill risk at higher ctx.

## Instrumented revalidation + per-process VRAM truth (15:43–, run `val-iq1s-2`)

Rerun at ceiling 8188 (keepout 0) with a side probe (2 s CSV: `nvidia-smi --query-gpu` +
compute-apps) → `C:/Users/allan/AppData/Local/Temp/vram-probe-iq1s.csv`. Issue #72 filed
with the full evidence chain.

- Device-wide idle baseline ~910 MB (desktop/WDDM); model load: 910 → 7853 MB stable.
- **Windows GPU Process Memory counters (PID 1113980): llama-server holds Dedicated
  7145.9 MB + Shared 586.0 MB = 7732 MB.** The model does NOT fully fit at 64k — 586 MB of
  model memory is demoted to system RAM (WDDM). The "shared = desktop only" hypothesis was
  wrong; Task Manager's 7.7 GB was the model's combined footprint.
- Bench 28.2 t/s and agentic scores therefore measure "Q1_S with 586 MB spilling". Unknown
  haircut vs hypothetical pure-VRAM 64k (impossible on 8.19 GB with this KV).
- Additional calibration fact: `nvidia-smi --query-gpu=memory.used` (7853) vs harness
  ctypes `nvmlDeviceGetMemoryInfo.used` (8038–8108 on same workload) disagree by ~250 MB —
  WDDM reserve accounting differs between the two "device-wide" paths; the watchdog uses
  the higher one.
- Operator decision consequence: since spill is already accepted at Q1, a higher quant
  (e.g. IQ2_XXS 7.3 GB, ~1.3–1.5 GB shared) trades more spill for a full bit of weight
  precision — candidate follow-up fingerprint. Alternatively 48k ctx fits fully dedicated
  (no shared) for a clean pure-VRAM comparison.

## Instrumented validation result (exit 0, 16:33) + full Trial launch

Validation **PASSED, completed clean** at ceiling 8188 — no watchdog event, no spill kill:

- agentic-quick **2/5, score 0.4000** (T010 1.00 PASS, T008 0.30, T002 0.30, T004 0.20,
  T006 0.20). The earlier 0/5 was a mid-run-kill artifact; with the server alive, tasks
  complete despite 3.5 t/s prefill.
- bench tg **28.4 t/s** (PASS ≥ 20), Peak VRAM **7.9 GB** (harness NVML) vs probe
  nvidia-smi 7853 MB — the ~250 MB device-wide tool delta, quantified on one run.
- Probe: peak 7853 MB @ 15:49 (during load), device-wide back to 668 MB after exit.
- Store: `TRIAL STATUS: incomplete` (validation row; axes not filled).

Full Trial `--agentic-full` launched 16:3x as managed process `trial-iq1s` (same
fingerprint: 64k turbo2, ceiling 8188, fallback sampler). Expected wall-clock 3–5 h at
measured prefill rate. Comparison target: Qwen3.8-4B-Distill (0.8667 agentic / 0.6400
coding / 74.9 tps @131k).

## Cancellation (operator, ~18:40)

Full Trial cancelled by operator at 2 h 04 m elapsed (wall-clock projection 3.5–5+ h:
agentic-quick pace 47.5 min/5 tasks, coding tail unknown). Verdict: **loser — move on.**

- Store record: validation row `83529912` (`outcome=OK`, `status=incomplete`,
  `val_score=0.4000`, `bench_tg=28.4`, `ctx=65536`, `memory_gb=7.9`). No full-Trial row —
  killed before any axis completed; per Trial rules the measured partial is preserved.
- Head-to-head vs `Qwen3.8-4B-Distill`: 27B IQ1_S loses every axis — ctx 64k (spills
  586 MB model-side to shared) vs 131k; prefill 3.5 t/s vs fast; bench 28.4 vs 74.9 t/s;
  quick 0.4000 vs 0.8667. Dense-27B-at-1-bit cannot beat distilled-4B on this rig.
- Baseline restored + verified to 4B-Distill winner. Probe CSV retained at
  `C:/Users/allan/AppData/Local/Temp/vram-probe-iq1s.csv`. Issue #72 (watchdog/labeling/
  calibration) remains open for the fix work.
