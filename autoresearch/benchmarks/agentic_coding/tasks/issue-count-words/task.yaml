id: issue-count-words
source_issue: synthetic-count
max_turns: 12
docs_only: false
allowlist:
  - words.py
issue: |
  ## Issue — word_count splits on spaces only

  `word_count(text)` must treat any whitespace as a separator (newlines, tabs).
  `"a\\nb\\tc"` should count as 3, not 1.

  Fix `words.py`.
